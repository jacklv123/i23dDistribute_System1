
SET(I23D_SRC_DIR ${CMAKE_CURRENT_LIST_DIR})
IF(MSVC OR APPLE)
    SET(I23D_BUILD_DIR "${EXECUTABLE_OUTPUT_PATH}/${CMAKE_BUILD_TYPE}")
ELSE()
    SET(I23D_BUILD_DIR ${EXECUTABLE_OUTPUT_PATH})
ENDIF()

set(SERVER_FILE "hosts")
CONFIGURE_FILE(${SERVER_FILE} "${CMAKE_CURRENT_BINARY_DIR}/hosts.txt" )


SET(I23DSFM_TUTORIAL_IN_FILE "${CMAKE_CURRENT_LIST_DIR}/run_i23d_DistributeSystem.py.in")
CONFIGURE_FILE(${I23DSFM_TUTORIAL_IN_FILE}
        "${CMAKE_CURRENT_BINARY_DIR}/run_i23d_DistributeSystem.py" @ONLY)

SET(MainFile "${CMAKE_CURRENT_LIST_DIR}/i23dDistributedSystem_run.py.in")
CONFIGURE_FILE(${MainFile}
        "${CMAKE_CURRENT_BINARY_DIR}/i23dDistributedSystem_run.py" @ONLY)


